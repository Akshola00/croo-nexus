// TODO: Phase 03 — On-Chain Verifier implementation
// Accepts CROO orders, runs 5 on-chain checks via Viem + Basescan + Tenderly,
// returns VerifierOutput with proofs. BAD_MODE=true returns wrong sourceVerified
// for the slash demo.

import 'dotenv/config'

console.log('[Verifier] Starting... (stub — Phase 03)')
